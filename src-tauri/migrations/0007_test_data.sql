-- ========================================
-- Migration 0007: Test Data (DISABLED)
-- ========================================
-- تم تعطيل هذا الملف لمنع إدراج بيانات اختبارية في الإنتاج
-- لتفعيل البيانات الاختبارية في التطوير، شغّل scripts/seed_dev_data.sql يدوياً
SELECT 1;
